# OpenAI API key — used for Whisper API transcription and LLM post-correction experiments.
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# DVC Azure remote connection string — stored in .dvc/config.local (not here).
# Copy from .dvc/config.local.example and fill in from the team vault.
# See docs/dvc-data-workflow.md for setup instructions.
